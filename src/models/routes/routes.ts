export enum PUBLIC {
  LANDING_PAGE = 'landingPage',
  DESCRIPTION = 'description',
  LOGIN = 'auth/login',
  REGISTER = 'auth/register',
}

export enum PRIVATE {
  PRIVATE = 'private',
  POKEDEX = 'pokedex',
  LOGOUT = 'auth/logout' 
}

