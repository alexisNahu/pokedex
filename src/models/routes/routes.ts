export enum PUBLIC {
  LANDING_PAGE = 'landingPage',
  DESCRIPTION = 'description',
  LOGIN = 'auth/login',
  REGISTER = 'auth/register',
  LOGOUT = 'auth/logout'
}

export enum PRIVATE {
  PRIVATE = 'private',
  POKEDEX = 'pokedex' 
}

